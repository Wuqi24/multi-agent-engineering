# Audit History（审计历史扩展）

> 可选扩展；核心 Workflow 不依赖本文件。

## 定位

- 结果层与流程层分离：结果（事实/证据/解释）保持确定性；流程记录（运行实例/时间戳/状态）可审计
- Ledger 只记录流程状态，不参与结果生成、评分或决策

## 落地要点

- 运行实例标识：由目标指纹 + 版本 + 时间戳派生
- 流程条目：引擎/规则状态 + 原因码 + 覆盖率 + 起止时间
- 历史记录：只存指纹与摘要，不保存完整结果
- 原因码冻结枚举，独立于状态
